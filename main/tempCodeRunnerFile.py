# ==============================================================================
# 1. 配置常量
# ==============================================================================
# --- OCR 配置 ---
# 定义了需要监控的屏幕区域 (左上角x, 左上角y, 宽度, 高度)
MONITOR_REGION = (895, 1003, 283, 45)  
# 定义了OCR需要识别的目标文字
TARGET_TEXT = "精通科目开启"
# 定义了识别出的文字与目标文字的相似度阈值，高于此值才算成功
